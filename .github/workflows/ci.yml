name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5

      - name: Build app and DMG
        run: make build

      - name: Test (native engines — runners have no MS Office)
        run: make test-native
